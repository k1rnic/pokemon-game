import React, { FC } from 'react';
import Layout from '../../components/Layout';

const AboutPage: FC = () => {
  return <Layout id="aboutSection" title="About game"></Layout>;
};

export default AboutPage;
