import React, { FC } from 'react';
import Layout from '../../components/Layout';

const NotFound: FC = () => {
  return <Layout id="notFoundSection" title="404"></Layout>;
};

export default NotFound;
