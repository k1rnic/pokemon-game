import React, { FC } from 'react';
import Layout from '../../components/Layout';

const ContactPage: FC = () => {
  return <Layout id="contactsSection" title="Contacts"></Layout>;
};

export default ContactPage;
